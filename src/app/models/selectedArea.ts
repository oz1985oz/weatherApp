export class SelectedArea {
    selectedAreaKey: string;
    selectedAreaName: string;
    constructor(selectedAreaKey: string, selectedAreaName: string) {
        this.selectedAreaKey = selectedAreaKey;
        this.selectedAreaName = selectedAreaName;
    }
}
